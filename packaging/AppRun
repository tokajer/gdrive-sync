#!/bin/sh
# AppRun entrypoint for the gdrive-sync AppImage.
HERE="$(dirname "$(readlink -f "$0")")"
export PATH="$HERE/usr/bin:$PATH"
# Point the app at the bundled rclone binary.
export GDRIVE_RCLONE="$HERE/usr/lib/gdrive-sync/rclone"
exec "$HERE/usr/bin/gdrive-sync" "$@"
